# Chat Service

This is a gRPC chat service that handles chat creation, message sending and chat deletion.

# CI

It uses the shared GO CI flow from `mihailtudos/actions/.github/workflows/go-ci.yml`.
